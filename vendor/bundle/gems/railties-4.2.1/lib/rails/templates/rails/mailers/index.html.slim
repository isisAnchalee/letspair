- @previews.each do |preview|
  h3= link_to preview.preview_name.titleize, "/rails/mailers/# preview.preview_name "
  ul
    - preview.emails.each do |email|
      li= link_to email, "/rails/mailers/# preview.preview_name}/#{email "
